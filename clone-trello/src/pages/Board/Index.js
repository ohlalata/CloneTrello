import React from "react";
import { Outlet } from "react-router-dom";

const BoardPages = () => {
  return (
    <React.Fragment>
      <div>AAAA</div>
      <Outlet />
    </React.Fragment>
  );
};

export default BoardPages;
